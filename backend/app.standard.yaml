runtime: python39
entrypoint: gunicorn -w 4 -b :$PORT main:app

service_account: _

# add only while deploying!
env_variables:
  COOKIE_DOMAIN: _
  FRONTEND_URL: _
  BACKEND_URL: _
  client_secret:
  client_id:
  POSTGRES_PATH:
  JWT_SECRET: